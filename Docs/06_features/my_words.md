# マイ単語（My Words）

対応要件: FR-92〜FR-97
実装（M7-A で入ったもの）:
`ui/screens/my_words_screen.dart`（SCR-17）,
`ui/screens/write_meaning_screen.dart`（「訳を書く」モード）,
`ui/dialogs/quick_add_word_sheet.dart`（クイック登録シート）,
`data/repositories/word_repository.dart`（`createOwned` / `watchDraftCount`）,
`data/repositories/wordbook_repository.dart`（`myWordsBookOf`）,
`domain/usecases/study_queue_builder.dart`（`StudyCandidate.isOwned` / `createdAt`）

他アプリからの共有受信（M7-D で追加）:
`domain/usecases/shared_text_parser.dart`（解釈の純粋関数）,
`domain/services/shared_text_source.dart`（受信元の抽象）＋
`data/services/shared_text_source_impl.dart`（`receive_sharing_intent`）,
`application/shared_text_receiver.dart`（起動時共有とストリームを1本にまとめる）,
`ui/widgets/shared_text_listener.dart`（`app.dart` に1度だけ差し込み、
クイック登録シートを開く）。
iOS の Share Extension / App Group は未実施（[Docs/08_platform_setup.md] §3.2）。

## 1. 狙い

教科書・問題集・洋書・動画で**自分が実際に出会った語**は、単語帳から与えられた語より圧倒的に覚えやすい。
出会った文脈がそのまま記憶の手がかりになるため。

社会人になってからも、仕事で出会った語を入れ続けられる。
プリセット単語帳を使い切った後もアプリが役に立ち続けるための機能でもある。

## 2. 所有と可視範囲

- `words` に `ownerProfileId`（int?, FK, index）を足す。
  null = 共有（プリセット・インポート）、非 null = そのプロファイルのマイ単語。
- 辞書一覧に出るのは **共有の語 ＋ 自分のマイ単語**。他人のマイ単語は出さない。
- 各プロファイルに「マイ単語」単語帳を1冊自動で持たせる
  （`source = user`、`presetId = null`、削除不可）。プロファイル作成時に一緒に作る。

同じ `(headword, partOfSpeech)` を兄と弟が別々に登録した場合、**2行になる**。
共有マスタの一意制約は `ownerProfileId` を含めた
`UNIQUE(headword, partOfSpeech, ownerProfileId)` にする。
片方のメモや訳をもう片方に見せないため、ここは重複を許す。

## 3. 未整備状態（下書き）を許す

出会った瞬間に訳や例文まで書かせると、登録そのものが面倒になって続かない。
**見出し語だけで登録できる**ようにする。

- `words` に `isDraft`（bool, default false）を足す。
- `isDraft = true` の語は `meaning` が空でよい（リポジトリで検証を分岐する）。
- 下書きの語は**出題されない**。キュー生成のプールから外す。
  訳の無い語を出しても答え合わせができない。
- 辞書一覧で `accentSoft` の「下書き」バッジを付け、フィルタ「下書きのみ」で絞り込める。
- ホームに下書きが3語以上たまったら「下書きの単語が5語あります。訳を書きましょう」のカードを出す。

### 3.1 訳は自分で書く

自動翻訳・辞書引きの API は使わない。

- 通信を伴うため、オフラインで完結するという前提が崩れる。
- そして何より、**自分の言葉で訳を書くこと自体が学習になる**。
  辞書の語義をそのまま貼るより、文脈に合う訳を自分で選んだ方が定着する。

出会った文を書き残せるよう、`exampleEn` に「出会った文」をそのまま入れることを勧める
（入力欄のヒントを「この単語を見つけた文」にする）。

## 4. クイック登録

### 4.1 アプリ内から

辞書画面のヘッダー `＋単語` と、シェル FAB の長押しから開く。

```
単語を追加                                (sectionTitle)
  [ apple                    ]           ← アプリ内英字キーボードで入力
  品詞  [ 名詞 ▾ ]                        （未定 も選べる）
  訳    [                    ]           ← 空でよい（下書きになる）
  見つけた文 [                ]           ← 任意
  [ 保存 ]   [ 保存してもう1語 ]
```

- 見出し語の入力にも**アプリ内英字キーボード**を使う。ここだけ OS の IME を出すと
  配列が変わって使いにくく、予測変換で綴りを確認できてしまう。
- 「保存してもう1語」でシートを閉じずに連続登録できる。授業中に何語も出てくる場面向け。
- 共有の語と同じ `(headword, partOfSpeech)` がすでにある場合は、新規作成せずに
  「この語は『中学英単語』にあります」と示し、**マイ単語に登録するか、既存の語を学習対象にするか**を選ばせる。
  この分岐に入っている間は通常の保存導線を隠し、必ずどちらかを明示的に選ばせる
  （推測でどちらかに倒さない）。
  「既存の語を学習対象にする」は、その共有語を**自分のマイ単語帳（`wordbooks.category = myWords`）へ
  所属させる**実装にした。新しい行を作らずに済み、そのマイ単語帳を学習対象にしていれば
  次のキュー生成から出題対象に入る。

呼び出し口: 辞書画面ヘッダーの「＋単語」と、シェル FAB の長押し。
辞書ヘッダーの「＋単語」は `upsert_word_sheet`（共有語の追加・編集用）から
クイック登録シートへ差し替えた。単語帳の文脈が無い状態からの追加は
「誰が持つ語なのか」が曖昧になりやすく、マイ単語として扱う方が実態に合うため
（単語帳の中身画面 SCR-13 の「＋」は、その単語帳という文脈があるので
引き続き `upsert_word_sheet` を使う）。
シェル FAB は「学習をはじめる」が短押しの主機能のままで、長押しでクイック登録を開く
（`GestureDetector.onLongPress` で `FloatingActionButton.extended` を包む）。
出題できる語が無く FAB 自体が非表示のときは、長押しでのクイック登録も使えない。
その間は辞書ヘッダーからのクイック登録が唯一の入口になる。

### 4.2 他アプリからの共有

OS の共有シートでテキストを受け取る（`receive_sharing_intent`）。
ブラウザや電子書籍で単語を選択 →「共有」→ encello を選ぶ、で登録シートが開く。

| 受け取ったテキスト | 挙動 |
|---|---|
| 1語（英字のみ） | 見出し語に入れてシートを開く |
| 複数語の文 | 文を「見つけた文」に入れ、**文中の語をチップで並べて選ばせる** |
| 日本語を含む | 「見つけた文」にだけ入れ、見出し語は空でシートを開く |

- 推測で見出し語を決めない。複数語のときは必ず選ばせる。
- プロファイルが2人以上のときは、シートの先頭に「だれのマイ単語にしますか」を出す。
  共有からの起動ではプロファイルゲートを通っていないため。

## 5. マイ単語画面（SCR-17）

設定 > マスタ、または辞書のフィルタ「マイ単語」から入る。
[STYLE_GUIDE §3] の一覧の型。単語帳フィルタは `WordbookFilter.myWords` 固定。

フィルタは「習熟度」「下書きのみ」の**2つのプルダウン**にした
（辞書の「単語帳」「習熟度」の並びと同じ位置に置く）。
「習熟度」の選択肢からは `下書きのみ` を外し（`MasteryFilter.draft` はここでは選ばせない）、
代わりに「下書き: すべて / 下書きのみ」の専用プルダウンを置く。ON のときは
「習熟度」の選択に関わらず下書きだけに絞る（2つの軸を独立させ、
「下書きだけ見たいのに習熟度で除外されて0件になる」事故を避ける）。

- 行タップで単語詳細（共有の語と同じ画面。編集・削除ができる）。
- FAB = クイック登録シート。
- 下書きが1語以上あるときだけ、画面下部（`bottomNavigationBar`）に
  「訳を書く（N語）」ボタンを出す（0語では出さない）。
  `bottomNavigationBar` に置くのは、`floatingActionButton` と重ならないよう
  Scaffold に自動でレイアウトさせるため。
- 下書きの語だけを順に埋めていく「訳を書く」モード（`write_meaning_screen.dart`）を置く。
  見出し語と見つけた文を出し、訳の入力欄だけを並べて次々に埋められるようにする。
  「あとで」で1語飛ばせる。全部終わったら完了表示にして戻れるようにする。

### 5.1 一括操作は v1 の範囲外

- 「選択してまとめて学習対象にする」「まとめて削除」は**このバージョンでは実装しない**。
  - マイ単語は登録の都度クイック登録シートで単語帳（自分のマイ単語帳）へ自動所属するため、
    「あとからまとめて学習対象にする」需要が M7-A の時点では薄い。
  - 一覧画面へ複数選択 UI（チェックボックス・選択モード切り替え・アクションバー）を
    新たに導入する必要があり、[STYLE_GUIDE §3] の一覧の型に無い新規パターンになる。
    先に本書（STYLE_GUIDE）側で型を定めてから実装する方針（[STYLE_GUIDE 本書の運用]）のため、
    今回は見送った。
  - 解除条件: 複数選択 UI の共通パターンを STYLE_GUIDE に追加したら実装する。
    削除は個別に単語詳細から行える（`confirmDestructive`）ため、当面はそちらで代替する。

## 6. 学習への組み込み

- マイ単語帳を学習対象に選べば、通常のキュー生成に混ざる（[srs_scheduler.md] §6）。
- 下書きは除外される。
- マイ単語は掲載順ではなく**登録が新しい順**に新規出題する。
  出会った直後に復習するのが最も効く。

## 7. エクスポート

- JSON エクスポートにマイ単語を含める。`ownerProfileId` はプロファイル名で書き出し、
  インポート時に名前で解決する（id は端末間で一致しないため）。
  同名のプロファイルが無ければ、インポート時に作る。
- CSV エクスポートはマイ単語帳も対象にする。

## 8. テスト観点

- 兄のマイ単語が弟の辞書一覧に出ない。
- 同じ `(headword, partOfSpeech)` を2人が登録すると2行になる。
- `isDraft = true` かつ `meaning` が空で保存できる。
- 下書きの語がキュー生成に含まれない。
- 訳を入力すると `isDraft` が false になり、出題対象に入る。
- 共有テキストが1語のとき見出し語に入り、文のときは語の選択になる。
- 日本語を含むテキストで見出し語が空のままになる（推測しない）。
- 共有の語と同じ見出し語＋品詞を登録しようとすると、既存を示す分岐に入る。
- マイ単語の新規出題順が登録の新しい順になる。
- エクスポート → 別端末インポートでマイ単語が正しいプロファイルに入る。
