# Encello - 開発ガイドライン & 開発規約

## 開発規約
- **設計書の同期**: 機能追加・仕様変更・リファクタリング時は、必ず `docs/` 配下の該当設計書を事前にまたは同時に更新すること。
- **README.mdとDocsの分離**: `README.md` はエンドユーザー向けの説明とし、開発に関する仕様・アーキテクチャ・メモはすべて `docs/` 配下に残すこと。
- **スタイルガイド**: レイヤー化アーキテクチャを守り、不必要な密結合や直打ち定数を避ける。
- **フォールバック・場当たり的対応の禁止**: 例外を握りつぶしたり、場当たり的な `try-catch` やダミー値返却でエラーを隠蔽しないこと。
- **ライブラリ最新化方針**:
  - 依存ライブラリは常に最新の安定版を採用する。
  - 定期的に `flutter pub outdated` を確認し、バージョンアップを検討・実行する。
  - バージョン固定が必要な場合は、その理由と解除条件を `docs/` 配下に明記する。

## Git & バージョン管理ルール
- コマンドチェイン（`&&`）を使用した一括実行は禁止。コマンドは1行ずつ実行すること。
- タスク完了時・主要ステップ完了ごとにローカルGitコミットを記録すること。
